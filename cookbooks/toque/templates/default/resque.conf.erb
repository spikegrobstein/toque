<% if @default_start %>
  start on runlevel [345]
  stop on runlevel [!345]
<% end %>

pre-start script

bash << "EOF"
  mkdir -p <%= @shared_path %>/log
  chown -R <%= @deploy_user %> <%= @shared_path %>/log
EOF

end script
